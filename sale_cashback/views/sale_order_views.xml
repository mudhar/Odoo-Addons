<?xml version="1.0"?>
<openerp>
    <data>

        <record id="sale_order_view_form_inherit_cash_back" model="ir.ui.view">
            <field name="name">sale.order.view.form.inherit.cash.back</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="arch" type="xml">
                <field name="user_id" position="after">
                    <field name="cash_back" attrs="{'readonly': [('state','in',('done','cancel'))]}"/>
                </field>
            </field>
        </record>
    </data>
</openerp>
