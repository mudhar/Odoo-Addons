<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Inherit Form View to Modify it -->
        <record id="view_picking_form_inherit_multi_sequence" model="ir.ui.view">
            <field name="name">Select Multi Sequence</field>
            <field name="model">stock.picking</field>
            <field name="inherit_id" ref="stock.view_picking_form"/>
            <field name="arch" type="xml">

                <xpath expr="//field[@name='partner_id']" position="after">
                    <!-- Add your fields or attributes here -->
                    <field name="product_select_type" readonly="1"/>
                </xpath>

            </field>
        </record>

        <!-- Inherit Form View to Modify it -->
        <record id="view_move_form_inherit_internal_name" model="ir.ui.view">
            <field name="name">view.move.form.inherit.internal.name</field>
            <field name="model">stock.move</field>
            <field name="inherit_id" ref="stock.view_move_form"/>
            <field name="arch" type="xml">

                <xpath expr="//field[@name='origin']" position="after">
                    <!-- Add your fields or attributes here -->
                    <field name="internal_transfer_type_name"/>
                </xpath>

            </field>
        </record>
    </data>
</odoo>